{
  "extract_function": {
    "type": "chat",
    "opts": {
      "template": "Extract a function called `{{fname}}` from the following code that does as the name suggests. Place it above the existing code. Make use of the new method in the existing code.\n\nCode:\n```{{filetype}}\n{{input}}\n```\n\nImproved version:\n```{{filetype}}",
      "strategy": "edit",
      "params": {
        "model": "gpt-3.5-turbo",
        "stop": [
          "```"
        ]
      }
    },
    "args": {
      "fname": {
        "type": "string",
        "optional": "false"
      }
    }
  },
  "idiomatize": {
    "type": "chat",
    "opts": {
      "template": "Make the following code more idiomatic {{filetype}} code. Aim for high readibility while keeping it concise.\n\nCode:\n```{{filetype}}\n{{input}}\n```\n\nImproved version:\n```{{filetype}}",
      "strategy": "edit",
      "params": {
        "model": "gpt-3.5-turbo",
        "stop": [
          "```"
        ]
      }
    }
  },
  "shorten": {
    "type": "chat",
    "opts": {
      "template": "Make the following {{filetype}} code short, terse, and concise. Make sure not to change the behavior.\n\nCode:\n```{{filetype}}\n{{input}}\n```\n\nImproved version:\n```{{filetype}}",
      "strategy": "edit",
      "params": {
        "model": "gpt-3.5-turbo",
        "stop": [
          "```"
        ]
      }
    }
  }
}
